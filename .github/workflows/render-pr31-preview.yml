name: Temporary PR31 Visual Preview
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR #31 preview screenshots are already generated. This temporary workflow is frozen pending final review."
