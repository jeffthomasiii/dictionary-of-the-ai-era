name: Apply September 2026 provenance Batch 02
on:
  push:
    branches:
      - research/2026-09-provenance-batch-02
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: research/2026-09-provenance-batch-02
      - name: Apply and validate provenance Batch 02
        run: python tools/apply_provenance_batch_02_2026_09.py
      - name: Commit researched provenance records
        run: |
          git config user.name "Jeff Thomas III"
          git config user.email "Masterstudent@duck.com"
          git add data/provenance.json docs/ROADMAP.md docs/PROVENANCE-REVIEW-QUEUE-2026-09.md
          if ! git diff --cached --quiet; then
            git commit -m "Complete EpochLex provenance research Batch 02"
            git push origin research/2026-09-provenance-batch-02
          fi
